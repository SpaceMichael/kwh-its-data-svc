version: '3'

services:
  kwh-its-eform-svc:
    container_name: kwh-its-eform-svc
    image: kwh-its-eform-svc
    restart: unless-stopped
    ports:
      - 30000:8080
    environment:
      TZ: Asia/Hong_Kong
      SPRING_PROFILES_ACTIVE: dev
